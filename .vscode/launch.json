{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/src/main.go",
            "cwd": "${workspaceFolder}/bin",
            // Comment out if you need to setup environment variables for the module
            //"envFile": "${workspaceFolder}/.env",
            "args": [
              "pipelines", 
              "run", 
              "loginapp",  
              "--file=C:\\Code\\bin\\config.personal.yml", 
              "--debug"
            ]
        }
    ]
}